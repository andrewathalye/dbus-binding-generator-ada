with "dbusada";

project Test is
   for Source_Dirs use ("src/", "generated/");
   for Object_Dir use "obj/";
   for Exec_Dir use "bin";
   for Main use ("portal_desktop.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnatwa", "-gnatwe", "-gnatyy");
   end Compiler;
end Test;
